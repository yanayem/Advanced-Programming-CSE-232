public class Main {
    static void main(String[] args) {
        int[] array = {2, 3, -8, 7, -1, 2, 3};
        int currentsum = 0;
        int maxsum = array[0];
        vector<int> newarray;
        for(int i = 0; i < array.length; i++){
            currentsum+=array[i];
            maxsum= Math.max(currentsum,maxsum);
            push.newarray[maxsum];
        }
        for(int j = 0; j < newarray.length; j++){
            temp = newarray[0];
            if(newarray[0] > temp){
                temp = newarray[j];
                
            }
        }

    }
}